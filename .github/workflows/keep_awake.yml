name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Streamlit
        run: curl -sSf https://ncyu-carbon-app-mduue5hffp7uknsskmjet9.streamlit.app/_stcore/health || true
